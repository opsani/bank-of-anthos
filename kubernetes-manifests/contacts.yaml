# Copyright 2019 Google LLC
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#      http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

apiVersion: apps/v1
kind: Deployment
metadata:
  name: contacts
spec:
  selector:
    matchLabels:
      app: contacts
  template:
    metadata:
      labels:
        app: contacts
    spec:
      serviceAccountName: default
      terminationGracePeriodSeconds: 5
      containers:
      - name: contacts
        #image: gcr.io/bank-of-anthos/contacts:v0.4.1
        imagePullPolicy: Always
        image: docker.io/danielhhowell/bofa-contacts
        command: [ "pyagent", "run", "-c", "/appdynamics/appd.cfg", "--", "gunicorn", "-b", ":8080", "--threads", "4", "--log-config", "/logging.conf", "--log-level=info", "contacts:create_app()" ]
        volumeMounts:
        - name: publickey
          mountPath: "/root/.ssh"
          readOnly: true
        - name: appdynamics
          mountPath: /appdynamics
        env:
        - name: VERSION
          value: "v0.4.1"
        - name: PORT
          value: "8080"
        - name: ENABLE_TRACING
          value: "false"
        # Valid levels are debug, info, warning, error, critical.
        # If no valid level is set, will default to info.
        - name: LOG_LEVEL
          value: "info"
        envFrom:
        - configMapRef:
            name: environment-config
        - configMapRef:
            name: accounts-db-config
        - configMapRef:
            name: appd-config
        resources:
          requests:
            cpu: 100m
            memory: 64Mi
          limits:
            cpu: 500m
            memory: 256Mi
        readinessProbe:
          httpGet:
            path: /ready
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 10
      volumes:
      - name: publickey
        secret:
          secretName: jwt-key
          items:
          - key: jwtRS256.key.pub
            path: publickey
      - name: appd-scripts
        configMap:
          name: appd-pyagent
      - name: appdynamics
      initContainers:
        - image: busybox:latest
          name: appd-pyagent
          env:
            - name: APPDYNAMICS_AGENT_NODE_NAME
              value: contacts
            - name: APPDYNAMICS_AGENT_ACCOUNT_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  key: accesskey
                  name: appd-secrets
            - name: APPDYNAMICS_AGENT_ACCOUNT_NAME
              valueFrom:
                secretKeyRef:
                  key: accountname
                  name: appd-secrets
            - name: APPDYNAMICS_CONTROLLER_HOST_NAME
              valueFrom:
                secretKeyRef:
                  key: hostname
                  name: appd-secrets
          envFrom:
            - configMapRef:
                name: appd-config
          command: ["/bin/sh", "/scripts/pyagent.sh"]
          volumeMounts:
            - mountPath: /scripts/pyagent.sh
              name: appd-scripts
              subPath: pyagent.sh
              readOnly: true
            - mountPath: /appdynamics
              name: appdynamics
---
apiVersion: v1
kind: Service
metadata:
  name: contacts
spec:
  type: ClusterIP
  selector:
    app: contacts
  ports:
  - name: http
    port: 8080
    targetPort: 8080
